import { connect } from 'react-redux'

import FooterComponent from '../../components/internal/FooterComponent';

const mapState = state => ({

});

const mapDispatch = dispatch => ({

});

const FooterContainer = connect(mapState, mapDispatch)((FooterComponent))


export default FooterContainer;